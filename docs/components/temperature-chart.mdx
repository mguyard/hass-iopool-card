---
title: Temperature chart
description: The native SVG chart used to display temperature history.
previous: /components/boost-selector
previousTitle: Boost selector
next: /modes/standard
nextTitle: Standard mode
---

# Temperature chart

The temperature chart renders the sensor history as a native SVG plot.

- It supports 24h, 48h, 96h, and 7d windows.
- It shows min, average, and max statistics under the graph.
- Hover or tap to inspect a point in the history.
