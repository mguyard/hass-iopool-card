---
title: Actions
description: Override the tap, hold, and double tap actions for each section.
previous: /configuration/chart
previousTitle: Chart
next: /components
nextTitle: Header
---

Use `section_actions` to customize interactions per section. The card does not expose a global card action because it represents a single pool.

Supported actions are `more-info` (default), `navigate`, `url`, `call-service`, `toggle`, and `none`.

```yaml
section_actions: # Optional
  temperature:
    tap_action:
      action: more-info
  temperature:
    tap_action:
      action: more-info
```
